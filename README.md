# TPLSAASCRM Backend

This project is the backend for the TPLSAASCRM application. It is built with FastAPI and connects to a PostgreSQL database.

## Development

Run `pip install -r requirements.txt` to install dependencies, then `uvicorn main:app --reload` to start the development server.